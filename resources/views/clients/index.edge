@layout('main')

@section('content')
  <h1>{{ email }}</h1>
  @if(old('notification'))
    <div class="alert alert-success">
      {{ old('notification') }}
    </div>
  @endif
  <ul class="list-group">
    @each(client in clients)
      <li class="list-group-item"><a href="/clients/{{client.id}}">{{ client.email }}</a></li>
    @endeach
  </ul>
@endsection